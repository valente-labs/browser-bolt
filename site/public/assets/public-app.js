'use strict';
const brand = window.QWEREBRAS_BRAND;
const publicRelease = window.BROWSER_BOLT_RELEASE;
const main = document.querySelector('main');
const esc = value => String(value ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const notice = text => `<div class="notice"><span class="status-dot" aria-hidden="true"></span><p>${text}</p></div>`;
const button = (href, label, primary = false) => `<a class="button${primary ? ' primary' : ''}" href="${href}">${label}</a>`;
const setupLink = () => button('/start', 'Set up Browser Bolt', true);
function home() {
  return `<div class="page"><section class="hero"><div class="hero-copy"><p class="availability"><span class="status-dot" aria-hidden="true"></span> Open-source BYOK preview</p><h1>5× Astra’s speed.<br>7× Opus’s speed.</h1><p class="lead">Jev + OpenRouter Qwen completed our synthetic browser tasks in a fraction of the time. Six attempts per route across three tasks. Your MCP host executes and verifies each action.</p><div class="actions">${setupLink()}${button('/docs/NATIVE_COMPARISON.md','See measured results')}</div><p class="small muted">Headline figures simplified from the September 18 synthetic benchmark; 7× is rounded. Native browser executor, no decision fallback. Results do not establish production MCP-host speed or equal capability on complex tasks. Requires Python 3.12+, provider credentials, and a browser-capable MCP host.</p></div><div class="loop" aria-label="Browser decision architecture"><div class="loop-heading"><span>One browser action</span><span class="tag">MCP</span></div><div class="loop-node"><span class="step">1</span><div><strong>Observe</strong><p>Your host supplies current page actions.</p></div><span class="ownership">Host</span></div><div class="connector" aria-hidden="true"></div><div class="loop-node selected"><span class="step">2</span><div><strong>Choose</strong><p>Jev selects. Qwen writes when needed.</p></div><span class="ownership">${esc(brand.name)}</span></div><div class="connector" aria-hidden="true"></div><div class="loop-node"><span class="step">3</span><div><strong>Act</strong><p>Your host checks and executes the action.</p></div><span class="ownership">Host</span></div><div class="connector" aria-hidden="true"></div><div class="loop-node"><span class="step">4</span><div><strong>Verify</strong><p>Your host checks the actual outcome.</p></div><span class="ownership">Host</span></div><p class="loop-note">Selected page context goes to cloud providers.<br>Browser execution stays with your host.</p></div></section>
  <section class="evidence" aria-labelledby="evidence-title"><div class="section-heading"><div><h2 id="evidence-title">Measure the whole task.</h2><p>Mean task time, including execution and independent verification.</p></div><a href="/docs/NATIVE_COMPARISON.md">Read the native results</a></div><div class="chart" role="img" aria-label="Measured mean task times: Jev plus OpenRouter Qwen 2.695 seconds, Astra 15.666 seconds, Opus 5 18.802 seconds. Common zero to twenty second axis."><div class="chart-row"><div><strong>Jev + OpenRouter Qwen</strong><span>One provider key</span></div><div class="track"><div class="bar fast"></div></div><strong>2.7s</strong></div><div class="chart-row"><div><strong>Astra</strong><span>Chat-only profile</span></div><div class="track"><div class="bar astra"></div></div><strong>15.7s</strong></div><div class="chart-row"><div><strong>Opus 5</strong><span>Chat-only profile</span></div><div class="track"><div class="bar opus"></div></div><strong>18.8s</strong></div><div class="axis"><span>0 seconds</span><span>20 seconds</span></div></div><p class="evidence-note">September 18, 2026. Six attempts per route across three synthetic tasks, two runs each. The paired profile used no decision fallback. Full cohort: 47/48 verified successes. A small controlled sample, not a production latency or complex-task guarantee.</p><details><summary>Inspect cost and comparison limits</summary><p>Mean model API cost per attempt: Jev + OpenRouter Qwen $0.0006396; Astra $0.050060; Opus 5 $0.0443267. These exclude hosting, support, payment fees, and service overhead. All attempts remain in averages, including failures.</p><p>The OpenRouter Qwen and direct Cerebras routes are distinct. These numbers do not establish equal capability on complex tasks or measure the hybrid fallback policy.</p></details></section>
  <section class="evidence" aria-labelledby="cost-title"><div class="section-heading"><div><h2 id="cost-title">Compare the API bill, too.</h2><p>Mean model cost per attempt on the same three-task cohort.</p></div></div><div class="cost-comparison" role="img" aria-label="Mean API cost per attempt: Jev plus OpenRouter Qwen 0.0006396 dollars, Jev plus Cerebras Qwen 0.0012389 dollars estimated, Astra 0.050060 dollars, Opus 5 0.0443267 dollars. Common zero to 0.06 dollar axis."><div class="cost-row"><strong>Jev + OpenRouter Qwen</strong><div class="track"><div class="cost-bar cost-or"></div></div><span>$0.000640</span></div><div class="cost-row"><strong>Jev + Cerebras Qwen</strong><div class="track"><div class="cost-bar cost-cerebras"></div></div><span>$0.00124*</span></div><div class="cost-row"><strong>Astra</strong><div class="track"><div class="cost-bar cost-astra"></div></div><span>$0.0501</span></div><div class="cost-row"><strong>Opus 5</strong><div class="track"><div class="cost-bar cost-opus"></div></div><span>$0.0443</span></div></div><p class="evidence-note">Common $0–$0.06 axis. Six attempts per route. OpenRouter reported costs; *direct Cerebras cost estimated at the documented token rates. Model API cost only, excluding browser hosting, support, taxes and service overhead. All attempts included.</p><details><summary>Where does Jev alone fit?</summary><p>Jev alone completed one separate choice-only MCP smoke in 1.148s for $0.000138978. It cannot generate field values in this implementation, so it is not ranked against the writing-task cohort above. The Jev + Cerebras Qwen route averaged 1.967s in the cohort; the one-key OpenRouter route averaged 2.695s. These are different provider routes.</p></details></section>
<section class="demo-section" id="browser-bolt-demo" aria-labelledby="demo-title">
  <h2 id="demo-title">Watch setup and a real browser task.</h2>
  <p>Four routes, original task footage and the measured API bill.</p>
  <figure class="demo-desktop">
    <video controls playsinline preload="none" width="1920" height="1080" poster="/assets/demo/poster.jpg" aria-label="Browser Bolt: full setup and four live routes" aria-describedby="demo-desktop-description">
      <source src="/assets/demo/browser-bolt-demo.mp4" type="video/mp4">
      <track kind="captions" src="/assets/demo/captions.en.vtt" srclang="en" label="English">
      Your browser cannot play this video. <a href="/assets/demo/browser-bolt-demo.mp4">Download the full demo</a>.
    </video>
    <figcaption id="demo-desktop-description">83-second silent demo. Use fullscreen to inspect fields and times. Captions are available in the player. <a href="/assets/demo/transcript.html">Read the full demo transcript</a>.</figcaption>
  </figure>
  <figure class="demo-mobile">
    <video controls playsinline preload="none" width="1080" height="1920" poster="/assets/demo/poster-mobile.png" aria-label="Browser Bolt: short portrait demo" aria-describedby="demo-mobile-description">
      <source src="/assets/demo/browser-bolt-demo-mobile.mp4" type="video/mp4">
      <track kind="captions" src="/assets/demo/captions.mobile.en.vtt" srclang="en" label="English">
      Your browser cannot play this video. <a href="/assets/demo/browser-bolt-demo-mobile.mp4">Download the portrait demo</a>.
    </video>
    <figcaption id="demo-mobile-description">53-second silent portrait demo. Two routes at a time for a closer view. Captions are available in the player. <a href="/assets/demo/transcript-mobile.html">Read the portrait demo transcript</a>. <a href="/assets/demo/browser-bolt-demo.mp4">Watch the full 83-second demo</a>.</figcaption>
  </figure>
</section>

  <section class="implementation"><div><h2>Keep the browser in your stack.</h2><p>The MCP server returns an operation, an observed action ID, field text when needed, and usage. Your host keeps control of authorization, freshness checks, execution, and verification.</p><a href="/security">Read the current data boundary</a></div><div class="code-panel"><div class="code-title">From your local checkout</div><pre><code>uv sync --locked --extra mcp
uv run --extra mcp jev-qwerebras-mcp</code></pre><p>Set provider credentials in your host’s local environment. The server waits for MCP messages on stdin.</p></div></section>
  <section class="closing"><h2>Try the decision path.<br>Inspect every assumption.</h2><div>${button('/docs/MCP.md','Configure a local host')}${button('/pricing','View proposed pricing')}</div><p>Managed accounts and billing are planned. No managed accounts are offered on this site.</p></section></div>`;
}
function startPage() {
  return `<div class="page"><div class="reading intro"><p class="eyebrow">LOCAL SETUP · NO ACCOUNT REQUIRED</p><h1 class="page-title">Give your host a faster decision path.</h1><p class="lead">Choose a provider route, configure your local MCP host, then verify one synthetic task. Your API keys stay outside this website.</p></div><div class="start-grid"><section class="setup-steps" aria-label="Setup instructions"><h2>Start with one task.</h2><ol><li><strong>Prepare your environment</strong><p>Use Python 3.12+, uv and a browser-capable MCP host. ${publicRelease ? "Download the versioned wheel and verify its checksum, or clone the release source." : "Build the wheel from this checkout with <code>uv build</code>."}</p></li><li><strong>Choose a route and configure the host</strong><p>OpenRouter uses one provider key. The direct Cerebras route uses two. Add keys to your host’s secret settings, then copy the configuration.</p></li><li><strong>Observe, act, and verify</strong><p>The host sends observed page actions to Browser Bolt and executes its returned choice. Confirm the actual page changed. A DONE response alone does not prove success.</p></li></ol><a class="button primary" href="/docs/MCP.md">Read the host setup contract</a><p class="small muted">This page cannot detect your host connection. Use the <a href="/docs/HOST.md">synthetic reference host</a> to check transport and execution. Named desktop integrations require their own adapter and validation.</p><details><summary>What data goes to providers?</summary><p>Goals, page URLs, titles, selected text, field labels and values, and action history can leave your host. Evaluate with synthetic data. No password or authentication-code handling is provided.</p></details></section>${setupPanel()}</div></div>`;
}
function pricing() {
  return `<div class="page"><div class="reading intro"><h1 class="page-title">Bring your key. Pay your provider.</h1><p class="lead">Browser Bolt has no software fee. Run the local MCP with your own browser-capable host and provider account.</p></div><div class="pricing-grid"><section class="plan"><h2>BYOK developer preview</h2><p class="price">$0 <span>software fee</span></p><p>MIT-licensed source, observed-action decisions, field writing and per-call usage. Provider requests are billed by your provider, including some failed or cancelled requests.</p>${setupLink()}<p class="small muted">Start with the synthetic reference host. Broader site and desktop-host compatibility require separate testing.</p></section><section class="plan planned"><h2>Managed service</h2><p>Account-based access, managed keys and product-credit billing are planned. No subscription or purchased credits are offered here.</p><button class="button" disabled>Managed service unavailable</button></section></div><div class="reading pricing-note"><h2>Benchmark cost is model cost.</h2><p>Published comparisons exclude browser hosting, infrastructure, support, taxes and payment fees. Your workload and route determine usage. Read the <a href="/docs/NATIVE_COMPARISON.md">measured results and limits</a>.</p></div></div>`;
}
function security() {
  return `<div class="page reading"><h1 class="page-title">Know what leaves your host.</h1><p class="lead">Browser execution belongs to your host. Selected page context and goals go to the configured model providers.</p>${notice('Evaluation scope: public, synthetic, non-sensitive data only.')}<section><h2>The current boundary</h2><p>Your host sends an observed DOM action table to the local MCP server. The server uses Jev and the configured text model to return a proposed action. It does not open tabs, execute selectors, run page JavaScript, or perform browser mutations.</p><p>Page text, field context, goals, and action history may be sent to the external provider. Local traces can also contain this information. “Local” describes where the MCP process runs, not where every piece of data stays.</p></section><section><h2>Your host must verify.</h2><p>The host owns authorization, fresh observations, target identity, and the execution boundary. It must independently verify outcomes. A model’s DONE response is not proof that a task succeeded. Browser mutations must not be automatically retried.</p></section><section><h2>What is not implemented</h2><p>There is no credential broker, secret-reference boundary, or general redaction layer. The MCP service is not a prompt-injection security boundary. Do not send passwords, session tokens, private customer records, health data, legal files, or confidential work through this preview.</p><p>No managed accounts or payment collection are available in this release.</p></section><section><h2>Inspect the contract.</h2><p><a href="/docs/MCP.md">Read the MCP input schema and host responsibilities.</a> Use a dedicated browser profile and synthetic fixtures when evaluating the decision path.</p></section></div>`;
}
function publicLegal(privacy) {
  const support = publicRelease ? `<a href="${esc(publicRelease.repositoryUrl)}/issues">GitHub Issues</a>` : 'the repository issue tracker when the public release opens';
  return `<div class="page reading"><h1 class="page-title">${privacy ? 'Privacy notice' : 'BYOK preview terms'}</h1><p class="lead">Browser Bolt is an MIT-licensed local MCP from Valente Labs. No managed account or subscription is offered on this website.</p>${privacy ? `<section><h2>This website</h2><p>The static site has no signup form, payment collection, provider-key input, analytics script or tracking pixel. Browser Bolt does not create an account or set account-session cookies here. Cloudflare processes technical request information to deliver and protect the site under its <a href="https://www.cloudflare.com/privacypolicy/">privacy policy</a>. This is not a promise that the hosting provider keeps no logs.</p></section><section><h2>What the MCP sends</h2><p>The MCP runs on your computer. It sends the goals and page context supplied by your host to the configured inference providers. This can include page URLs, titles, text, field labels and values, and recent actions. The default route uses OpenRouter for Jev decisions and Qwen writing. Direct Cerebras profiles send their Qwen requests to Cerebras.</p><p>Your host and providers have their own data policies. Review <a href="https://openrouter.ai/docs/guides/privacy/data-collection">OpenRouter data collection</a>, <a href="https://openrouter.ai/docs/guides/privacy/provider-logging">downstream provider policies</a> and <a href="https://www.cerebras.ai/privacy-policy">Cerebras privacy</a>. This preview does not enforce a universal zero-retention setting or redact confidential content.</p></section><section><h2>Keys and local records</h2><p>Provider keys come from your local host or process environment and authenticate provider requests. This website does not accept them. Your host may retain tool inputs and outputs; local diagnostics and recordings may contain page content. Use synthetic, non-sensitive inputs and review records before sharing. Browser Bolt does not handle passwords or authentication codes for you.</p></section>` : `<section><h2>Software and provider charges</h2><p>The software fee is $0. The <a href="/LICENSE.txt">MIT license</a>, including its warranty and liability provisions, governs the software. You use your own provider account and pay its charges directly. Failed or rejected responses and cancellation after dispatch can still incur charges. No purchased Browser Bolt credits or hosted execution service are included.</p></section><section><h2>Your browser host</h2><p>Your host observes the page, authorizes actions, executes them and checks the result. Treat model output and page text as untrusted. Independently verify target identity, freshness and task outcomes. A DONE response alone is not proof of completion. Do not automatically repeat browser mutations or cancelled provider requests.</p><p>Use public, synthetic, non-sensitive data during evaluation. This preview has no credential broker, general content redaction or prompt-injection security guarantee. Only claim compatibility for a host and platform you have tested.</p></section>`}<section><h2>Support and changes</h2><p>Report setup problems through ${support}, using your version, host and a synthetic reproduction. Keep keys and private page content out of reports. Support is best effort, with no guaranteed response time or uptime commitment. The managed-service roadmap has separate launch and policy gates.</p></section><p><a href="/security">Read the security boundary</a> and <a href="/docs/MCP.md">MCP contract</a>. Notice version: September 21, 2026.</p></div>`;
}
function setupPanel() {
  const download = publicRelease ? `<p><a class="button primary" href="${esc(publicRelease.wheelUrl)}">Download version ${esc(publicRelease.tag)}</a></p><p class="small"><a href="${esc(publicRelease.checksumsUrl)}">SHA256 checksums</a> · <a href="${esc(publicRelease.requirementsUrl)}">Locked dependencies</a>. Verify the wheel before installation. Follow the <a href="/docs/HOST.md">reference host guide</a> for a synthetic first task.</p>` : '';
  return `<section class="setup-panel">${download}<div class="section-heading"><h2>Connect your host</h2><span class="tag">BYOK</span></div><p>Run MCP locally from a built wheel. Add your provider key in your host’s environment settings, outside this page.</p><label for="profile">Provider route</label><select id="profile"><option value="jev_qwen_openrouter">Jev + OpenRouter Qwen · one key</option><option value="jev_qwen">Jev + Cerebras Qwen · two keys</option><option value="jev">Jev only · no field writing</option></select><p id="profile-note" class="small muted"></p><p class="small">Pass <code id="profile-value"></code> as the profile argument when calling <code>choose_browser_action</code> or <code>write_browser_field</code>.</p><div class="code-panel"><div class="code-title"><span>Host configuration</span><button id="copy-config" class="text-button" type="button">Copy config</button></div><pre><code id="host-config"></code></pre></div><p id="copy-status" class="status small" role="status"></p><p class="small muted">${publicRelease ? "This configuration uses the versioned release wheel. Use the locked-dependency instructions for a reproducible install." : "Replace the wheel path with a file you built locally."} The selected profile must also be passed to each MCP tool call. Copying this configuration does not connect a host.</p><a href="/docs/MCP.md">Follow the complete MCP setup</a></section>`;
}
function updateSetup() {
  const route = document.querySelector('#profile').value;
  document.querySelector('#profile-value').textContent = JSON.stringify(route);
  const keys = route === 'jev_qwen' ? 'OPENROUTER_API_KEY and CEREBRAS_API_KEY' : 'OPENROUTER_API_KEY';
  document.querySelector('#profile-note').textContent = `${keys} required in your local host environment.${route === 'jev' ? ' Jev alone does not write field values.' : ''}`;
  document.querySelector('#host-config').textContent = JSON.stringify({mcpServers:{[brand.slug]:{command:'uvx',args:['--from',publicRelease ? publicRelease.wheelUrl+'[mcp]' : '/absolute/path/to/jev_qwerebras_ultrafast-0.1.0-py3-none-any.whl[mcp]','jev-qwerebras-mcp']}}},null,2);
  document.querySelector('#copy-status').textContent = '';
}
function bindSetup() {
  updateSetup();
  document.querySelector('#profile').addEventListener('change',updateSetup);
  document.querySelector('#copy-config').addEventListener('click',async () => { const status = document.querySelector('#copy-status'); try { await navigator.clipboard.writeText(document.querySelector('#host-config').textContent); status.textContent = 'Configuration copied. Set credentials locally and follow MCP setup.'; } catch { status.textContent = 'Copy was unavailable. Select the configuration text and copy it manually.'; } });
}
function init() {
  document.title = brand.title;
  document.querySelectorAll('[data-brand]').forEach(node => { node.textContent = brand.name; });
  document.querySelector('.brand').setAttribute('aria-label', `${brand.name} home`);
  const path = window.location.pathname.replace(/\/$/, '') || '/';
  document.querySelectorAll('nav a').forEach(link => {
    if (link.getAttribute('href') === path) link.setAttribute('aria-current', 'page');
  });
  const routes = {'/': home, '/start': startPage, '/pricing': pricing, '/security': security, '/terms': () => publicLegal(false), '/privacy': () => publicLegal(true)};
  main.innerHTML = Object.hasOwn(routes, path) ? routes[path]() : `<div class="page narrow"><h1 class="page-title">Page not found.</h1><p>This route is not part of this release.</p>${button('/', 'Return to overview', true)}</div>`;
  if (path === '/start') bindSetup();
  if (path === '/') {
    const viewport = window.matchMedia('(max-width: 767px)');
    const pauseHidden = () => main.querySelectorAll('#browser-bolt-demo video').forEach(video => {
      if (!video.getClientRects().length) video.pause();
    });
    viewport.addEventListener('change', pauseHidden);
    pauseHidden();
  }
}
init();
